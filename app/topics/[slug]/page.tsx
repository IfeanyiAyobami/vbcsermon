import {notFound} from "next/navigation";import {sermons} from "@/data/sermons";import {topics} from "@/data/topics";import {Container} from "@/components/ui/Container";import {SermonCard} from "@/components/cards/SermonCard";
export function generateStaticParams(){return topics.map(t=>({slug:t.toLowerCase().replaceAll(" ","-")}))}
export default async function TopicPage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const topic=topics.find(t=>t.toLowerCase().replaceAll(" ","-")===slug);if(!topic)notFound();const results=sermons.filter(s=>s.topics.some(t=>t.toLowerCase()===topic.toLowerCase()));return <main className="min-h-screen bg-[var(--vbc-black)] py-20 text-white lg:py-28"><Container><p className="text-[11px] font-bold uppercase tracking-[.3em] text-[#ff0000]">Topic</p><h1 className="vbc-display mt-3 text-7xl uppercase leading-[.82]">{topic}</h1><div className="mt-12 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{results.length?results.map(s=><SermonCard key={s.id} sermon={s}/>):<p className="text-white/40">No sermons are currently assigned to this topic.</p>}</div></Container></main>}