import {NextRequest,NextResponse} from "next/server";import {supabase} from "@/lib/supabase/server";
export async function POST(req:NextRequest){try{const body=await req.json();if(!body.sermonId||!body.listenerId)return NextResponse.json({ok:false},{status:400});const {error}=await supabase.from("sermon_plays").insert({sermon_id:body.sermonId,listener_id:String(body.listenerId).slice(0,100),listened_seconds:Math.max(20,Math.min(Number(body.listenedSeconds)||20,86400))});if(error){console.error("play analytics:",error.message);return NextResponse.json({ok:false},{status:500})}return NextResponse.json({ok:true})}catch{return NextResponse.json({ok:false},{status:400})}}
