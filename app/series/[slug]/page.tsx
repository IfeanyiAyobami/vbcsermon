import {notFound} from "next/navigation";import {series} from "@/data/series";import {sermons} from "@/data/sermons";import {Container} from "@/components/ui/Container";import {SermonCard} from "@/components/cards/SermonCard";
export function generateStaticParams(){return series.map(s=>({slug:s.slug}))}
export default async function SeriesPage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const item=series.find(s=>s.slug===slug);if(!item)notFound();const items=sermons.filter(s=>s.seriesSlug===item.slug);return <main className="min-h-screen bg-[var(--vbc-black)] py-16 lg:py-24"><Container><p className="text-[11px] font-bold uppercase tracking-[.3em] text-red-400">{item.date}</p><h1 className="vbc-display mt-3 text-7xl uppercase leading-[.82] sm:text-9xl">{item.title}</h1><p className="mt-6 max-w-2xl text-white/50">{item.description}</p><div className="mt-14 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{items.map(s=><SermonCard key={s.id} sermon={s}/>)}</div></Container></main>}