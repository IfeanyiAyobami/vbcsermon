import Image from "next/image";import Link from "next/link";import {ArrowRight} from "lucide-react";import type {SermonSeries} from "@/data/series";
export function SeriesCard({item}:{item:SermonSeries}){return <Link href={`/series/${item.slug}`} className="vbc-card group relative min-h-[360px] overflow-hidden rounded-[28px] border border-white/10"><Image src={item.image} alt={item.title} fill className="vbc-card-image object-cover opacity-65" sizes="(max-width:768px) 100vw,50vw"/><div className="absolute inset-0 bg-gradient-to-t from-[#040451] via-[#040451]/30 to-transparent"/><div className="absolute left-6 top-6 rounded-full border border-white/20 bg-black/20 px-3 py-1.5 text-[10px] font-bold uppercase tracking-[.15em]">{item.count} messages</div><div className="absolute bottom-0 p-7 sm:p-8"><p className="text-[10px] font-bold uppercase tracking-[.25em] text-red-400">{item.date}</p><h3 className="vbc-display mt-3 text-5xl uppercase leading-[.85] sm:text-6xl">{item.title}</h3><p className="mt-4 max-w-lg text-sm leading-6 text-white/50">{item.description}</p><span className="mt-5 inline-flex items-center gap-2 text-sm font-bold">Explore series <ArrowRight size={16}/></span></div></Link>}