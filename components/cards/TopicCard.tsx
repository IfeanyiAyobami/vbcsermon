import Link from "next/link";export function TopicCard({topic,index}:{topic:string;index:number}){return <Link href={`/sermons?topic=${topic.toLowerCase().replaceAll(" ","-")}`} className="group flex items-center justify-between rounded-2xl border border-white/10 bg-white/[.035] p-5 transition hover:-translate-y-1 hover:border-[#ff0000] hover:bg-[#ff0000]"><span className="vbc-display text-2xl uppercase">{topic}</span><span className="text-xs text-white/25 group-hover:text-white">{String(index+1).padStart(2,"0")}</span></Link>}