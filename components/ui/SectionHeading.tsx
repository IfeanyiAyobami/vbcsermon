import Link from "next/link";import {ArrowUpRight} from "lucide-react";
export function SectionHeading({eyebrow,title,href,light=false}:{eyebrow:string;title:string;href?:string;light?:boolean}){return <div className={`flex flex-col gap-5 border-b pb-8 sm:flex-row sm:items-end sm:justify-between ${light?"border-black/10":"border-white/10"}`}><div><p className="text-[11px] font-bold uppercase tracking-[.32em] text-[#ff0000]">{eyebrow}</p><h2 className={`vbc-display mt-3 text-5xl uppercase leading-[.85] sm:text-6xl lg:text-7xl ${light?"text-[#170059]":"text-white"}`}>{title}</h2></div>{href&&<Link href={href} className={`group inline-flex items-center gap-2 text-sm font-semibold ${light?"text-[#170059]":"text-white/55"}`}>View all <ArrowUpRight size={16}/></Link>}</div>}